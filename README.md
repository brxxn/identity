# identity

This project is a work in progress, come back later!

## Notes

* `docker compose exec app /bin/identity setup` while container is running for first-time setup
* `docker compose exec app /bin/identity get-login-link` to get login link for a user